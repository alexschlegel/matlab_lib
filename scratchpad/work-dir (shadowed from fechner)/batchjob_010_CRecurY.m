
% This job is being run against the Tue Mar 24 19:00:16 2015 -0400
% version of Pipeline.m (commit 076f332).
%
% The present version of constructTestPlotData generates accuracy
% data as a function of WSum, as a function of WFullness, and as a
% function of nTBlock, in each instance with CRecurY equal to
% {0, 0.35, 0.7}.

pd1=Pipeline.constructTestPlotData('max_cores',12);

exit
